name: Publish Electron Forge Build to GitHub Releases

on:
  push:
    branches:
      - master  # Change this to your default branch

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install Dependencies
        run: npm install

      - name: Build Electron Forge for Windows
        run: npx electron-forge make --platform win

      - name: Archive Windows Build
        run: |
          mkdir -p releases
          mv ./out/make/win releases/
          cd releases
          zip -r electron-build-windows.zip win/
        shell: bash

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install Dependencies
        run: npm install

      - name: Build Electron Forge for macOS
        run: npx electron-forge make --platform darwin

      - name: Archive macOS Build
        run: |
          mkdir -p releases
          mv ./out/make/darwin releases/
          cd releases
          zip -r electron-build-macos.zip darwin/
        shell: bash

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install Dependencies
        run: npm install

      - name: Build Electron Forge for Linux
        run: npx electron-forge make --platform linux

      - name: Archive Linux Build
        run: |
          mkdir -p releases
          mv ./out/make/linux releases/
          cd releases
          zip -r electron-build-linux.zip linux/
        shell: bash

  create-release:
    runs-on: ubuntu-latest
    needs: [build-windows, build-macos, build-linux]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Check semver incremented value
        id: check_incremented_value
        run: |
          string="${{ github.event.head_commit.message }}"
          if [[ $string == *"#minor"* ]]; then
            echo "incremented_value=minor" >> $GITHUB_OUTPUT
          elif [[ $string == *"#major"* ]]; then
            echo "incremented_value=major" >> $GITHUB_OUTPUT
          else
            echo "incremented_value=patch" >> $GITHUB_OUTPUT
          fi

      - name: Create tag
        id: tag
        uses: SOLIDSoftworks/semver-tags@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag-prefix: 'v'
          default-version: 'v0.0.1'
          incremented-value: ${{ steps.check_incremented_value.outputs.incremented_value }}

      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.semantic-version }}  # Replace with your desired release tag
          release_name: Release ${{ steps.tag.outputs.semantic-version }}  # Replace with your desired release name
          draft: false
          prerelease: false

      - name: Upload Windows Build
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: releases/electron-build-windows.zip
          asset_name: electron-build-windows.zip
          asset_content_type: application/zip

      - name: Upload macOS Build
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: releases/electron-build-macos.zip
          asset_name: electron-build-macos.zip
          asset_content_type: application/zip

      - name: Upload Linux Build
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: releases/electron-build-linux.zip
          asset_name: electron-build-linux.zip
          asset_content_type: application/zip
